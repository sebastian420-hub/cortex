# Test dependencies for Cortex
# Includes base requirements and additional testing tools

# Base requirements
-r requirements.txt

# Testing framework
pytest>=7.0.0
pytest-cov>=4.0.0
pytest-mock>=3.10.0
pytest-asyncio>=0.21.0
pytest-xdist>=3.0.0
pytest-benchmark>=4.0.0
hypothesis>=6.0.0

# Mocking and test utilities
freezegun>=1.2.0
responses>=0.23.0
aioresponses>=0.7.0
docker>=6.0.0

# Security scanning
bandit>=1.7.0
safety>=2.0.0

# Code quality (already in requirements-dev.txt)
black>=23.0.0
flake8>=6.0.0
mypy>=1.0.0

# Optional: pre-commit hooks
pre-commit>=3.0.0