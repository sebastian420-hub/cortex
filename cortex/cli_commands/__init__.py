"""CLI subpackage for command system"""

# Note: This package contains the modular command system.
# The main CLI functionality is in cortex/cli.py (sibling module).

# For backward compatibility with tests, we don't export anything here.
# Tests should import from cortex.cli (the module), not cortex.cli (the package).
